# Pinned so the docs build is reproducible and a Material release cannot break a push without warning.
# Bump deliberately: `pip install -U mkdocs-material` locally, `mkdocs build --strict`, then update this line.
mkdocs-material==9.7.7
